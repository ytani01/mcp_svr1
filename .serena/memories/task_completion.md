# Task Completion

When a task is completed, run the following commands:

1.  **Linting:**
    - `uv run ruff check .`
    - `uv run mypy .`
    - `uv run pyright .`
2.  **Testing:**
    - `uv run python -m pytest -v`
